import re

input_file = r"f:\开发软件项目文件\对练社交\docs\prd.md"
output_file = r"f:\开发软件项目文件\对练社交\docs\prd_new.md"

with open(input_file, 'r', encoding='utf-8') as f:
    content = f.read()

emotion_service = """

### 3.4.1 情绪识别服务

**服务定位**：实时识别用户和教练的情绪状态，为个性化反馈和训练调整提供数据支撑。

**技术选型**：
| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 文本情绪分析 | DeepSeek-Chat（内置） | 分析用户文本输入中的情绪倾向 |
| 语音情绪分析 | Microsoft Azure Speech Service | 分析语速、语调、音量中的情绪特征 |
| 表情情绪分析 | MVP阶段暂不实现，预留接口 | 分析用户摄像头画面中的面部表情 |

**三层分析引擎**：

| 层级 | 分析内容 | 输出 | 权重 |
|------|---------|------|------|
| 文本层 | 关键词、语气词、句式结构 | 情绪类型+强度(0-100) | 60% |
| 语音层 | 语速(字/秒)、音调(F0)、音量(RMS) | 情绪类型+强度(0-100) | 30% |
| 表情层 | 面部表情特征点 | 情绪类型+强度(0-100) | 10% |

**情绪类型定义**：

| 情绪类型 | 关键词特征 | 语音特征 | 典型场景 |
|---------|----------|---------|---------|
| 开心 | "太棒了""太好了""哈哈" | 语速稍快、音调较高、音量适中 | 获得高分、完成训练 |
| 平静 | "好的""可以""我知道了" | 语速适中、音调平稳、音量适中 | 正常对话、理性回答 |
| 紧张 | "嗯..."停顿多、语无伦次 | 语速快、音调颤抖、音量小 | 面试场景、高压对话 |
| 焦虑 | "怎么办""我不行""好难" | 语速快、音调偏高、频繁停顿 | 社交焦虑、高难度场景 |
| 沮丧 | "算了""不想说了""没意思" | 语速慢、音调低、音量小 | 连续负分、训练失败 |
| 生气 | "什么""你怎么""太过分" | 语速快、音调高、音量大 | 冲突场景、被误解 |

**融合决策规则**：

综合情绪 = (文本层输出 × 0.6) + (语音层输出 × 0.3) + (表情层输出 × 0.1)

- 当三层分析结果一致时，直接采用综合结果
- 当三层分析结果不一致时，以文本层为主（权重最高）
- 当情绪强度<30时，判定为"微弱情绪"，不触发特殊响应
- 当情绪强度>70时，判定为"强烈情绪"，触发针对性干预

**情绪识别触发时机**：

| 触发时机 | 触发条件 | 响应动作 |
|---------|---------|---------|
| 每轮对话后 | 用户发送消息 | 分析用户情绪，更新情绪日记 |
| 得分变化时 | 正负分变化超过5分 | 分析情绪变化，调整教练反馈策略 |
| 训练结束时 | 训练完成 | 生成情绪总结，写入情绪日记 |
| 情绪异常时 | 连续3轮负面情绪 | 触发CBT引导或防沉迷提醒 |

**成本优化方案**：

| 策略 | 说明 | 预估效果 |
|------|------|---------|
| 文本分析复用LLM | 利用DeepSeek-Chat在对话分析时附带情绪分析，不额外调用 | 零额外成本 |
| 语音分析按需启用 | 仅在语音训练模式下启用，文字模式跳过 | 减少70%语音分析调用 |
| 表情分析延迟实现 | MVP阶段预留接口，正式版再集成 | 降低初期开发成本 |
| 情绪缓存 | 同一用户30秒内重复情绪分析结果缓存 | 减少重复计算 |
"""

old_pattern = r"(### 3\.5 LLM混合引擎架构)"
new_content = emotion_service + "\n" + r"\1"

content = re.sub(old_pattern, new_content, content)

collapsible_design = """
**UI折叠式设计：**
- 首页信息收拢为折叠式设计，减少一次性信息过载
- 用户点击"我的训练记录"、"本周进度"、"晚安计划"、"变美联动"后展开详细内容
- 未展开时显示简要信息（如训练次数、进度百分比、计划时间）
- 展开动画：平滑过渡，不影响用户体验
"""

old_pattern2 = r"(└─────────────────────────────┘\n```\n\n### 4\.2 Tab2 教练页)"
new_content2 = r"\1" + "\n\n" + collapsible_design

content = re.sub(old_pattern2, new_content2, content)

tips_system = """

### 4.6 每日小贴士系统

**定位**：每日为用户推送一条社交技巧小贴士，帮助用户在训练之外持续学习和成长。

**小贴士来源：**

**来源一：训练数据沉淀**
- 用户完成训练后，系统总结该场景的关键技巧，存入"小贴士库"
- 基于用户训练表现，推送针对性的改进技巧

**来源二：预设技巧库（MVP先做）**
| 场景分类 | 技巧示例 |
|---------|---------|
| 面试 | "STAR法则：先讲情境，再讲任务，然后讲行动，最后讲结果" |
| 相亲 | "问开放式问题：把'你喜欢旅游吗'换成'你印象最深的一次旅行是去哪里'" |
| 加薪 | "用数据说话：不要说'我工作很努力'，要说'我今年完成了X个项目'" |
| 日常 | "微笑是最好的开场白，真诚的笑容能瞬间拉近距离" |
| 共情 | "重复对方的感受：'听起来你当时一定很委屈'" |

**来源三：AI实时生成**
- 训练结束后，系统基于用户的对话历史，生成一条定制小贴士
- 定制小贴士包含用户本次训练的具体改进点

**推送机制：**
- 每日首次打开App时推送
- 显示位置：首页顶部个性化问候下方
- 每条小贴士下方增加【👍有用】【👎没用】按钮，收集反馈
- 优质内容（高有用率）优先推送给其他用户
- 用户可在"我的页→学习卡片"中查看历史小贴士

**小贴士卡片规格：**
```
💡 今日小贴士
━━━━━━━━━━━━━━━
问开放式问题
把"你喜欢旅游吗"换成"你印象最深的一次旅行是去哪里"

【👍有用】 【👎没用】
```
"""

old_pattern3 = r"(## 5\. AI教练系统)"
new_content3 = tips_system + "\n" + r"\1"

content = re.sub(old_pattern3, new_content3, content)

version_history = """| v2.2 | 2026-07-17 | 训练对话系统升级：动态深度对话、自由输入、实时建议、灵活结束条件；选项盲选+乱序；0-10分双向评分；净得分制；时空穿梭错误补救；学习卡片错误分析；情绪识别服务；UI折叠式设计；每日小贴士系统 |"""

old_pattern4 = r"\| v2\.2 \| 2026-07-17 \| 训练对话系统升级：动态深度对话、自由输入、实时建议、灵活结束条件 \|"
content = content.replace(old_pattern4, version_history)

with open(output_file, 'w', encoding='utf-8') as f:
    f.write(content)

print("PRD updated successfully!")
