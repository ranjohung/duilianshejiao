# 核心技术架构

> 来源：PRD v2.0 第3章

## 3. 核心技术架构

### 3.1 技术栈总览

```
┌─────────────────────────────────────────────────────┐
│                    客户端 (Flutter)                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │ Spine 2D │ │ Live2D   │ │ Three.js │ │  Agora  │ │
│  │  (MVP)   │ │ (周卡)   │ │  3D渲染  │ │  RTC    │ │
│  └──────────┘ └──────────┘ └──────────┘ └─────────┘ │
└─────────────────────────┬───────────────────────────┘
                          │ HTTPS / WSS
┌─────────────────────────┴───────────────────────────┐
│               后端 (Node.js + Express)                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │ LLM路由器│ │  TTS服务  │ │ 训练引擎 │ │ 定时任务│ │
│  │          │ │(Azure)   │ │          │ │(cron)   │ │
│  └──────────┘ └──────────┘ └──────────┘ └─────────┘ │
└────┬──────────────┬──────────────┬───────────────────┘
     │              │              │
┌────┴────┐  ┌─────┴─────┐  ┌────┴────┐
│ MySQL   │  │  Redis    │  │ 阿里云  │
│  9.6    │  │  缓存     │  │  OSS    │
└─────────┘  └───────────┘  └─────────┘
```

### 3.2 客户端技术

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 跨端框架 | Flutter | 一套代码覆盖iOS/Android |
| AI教练渲染(MVP) | Spine 2D | 4个教练立绘，5种基础表情动画 |
| AI教练渲染(周卡) | Live2D | 2.5D效果，更丰富的表情和动作 |
| AI教练渲染(月卡/年卡) | Three.js 3D | 全3D渲染，≥30fps |
| 实时音视频 | 声网Agora RTC | 全双工语音，延迟<500ms |

### 3.3 后端技术

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| 运行时 | Node.js | 高并发I/O |
| 框架 | Express | RESTful API |
| 数据库 | MySQL 9.6 | 主数据存储 |
| 缓存 | Redis | 会话管理、热点数据缓存、训练次数计数 |
| 对象存储 | 阿里云OSS | 用户头像、训练录音、分享卡片等文件 |
| 定时任务 | node-cron | 签到重置、每周报告生成、晚安计划触发、朋友圈自动发布 |
| 实名认证 | uni一键登录（首选）+ 阿里云实人认证 + uni实人认证（备选），系统自动识别优先用便宜的 | 注册即实名，合规要求 |

### 3.4 AI服务

| 组件 | 技术选型 | 说明 |
|------|---------|------|
| LLM(主力) | DeepSeek-Chat | 高质量对话生成，用于付费用户 |
| LLM(兜底) | Ollama + Qwen2.5:14B | 本地部署，用于免费用户和降级场景 |
| TTS | Microsoft Azure TTS | 多音色语音合成 |

### 3.5 LLM混合引擎架构

```
用户输入
  │
  ▼
┌───────────────┐
│   LLM路由器    │
│ (LLM Router)  │
└───────┬───────┘
        │
        ├── 体验用户(1次) ──────→ DeepSeek API
        │
        ├── 免费用户 ──────────→ Ollama + 10%偶遇DeepSeek
        │
        ├── 日卡/周卡/月卡 ────→ DeepSeek API
        │
        ├── 年卡 ──────────────→ DeepSeek API (高优先级队列)
        │
        └── 健康检查失败 ──────→ 自动降级Ollama
```

**路由规则详解：**

| 用户类型 | LLM分配策略 | 说明 |
|---------|------------|------|
| 体验用户 | DeepSeek（仅1次） | 首次体验使用高质量模型，展示产品价值 |
| 免费用户 | Ollama为主，10%概率偶遇DeepSeek | 控制成本，偶尔回升质量让用户体验差异 |
| 日卡用户 | DeepSeek | 付费享有高质量对话 |
| 周卡用户 | DeepSeek | 付费享有高质量对话 |
| 月卡用户 | DeepSeek | 付费享有高质量对话 |
| 年卡用户 | DeepSeek（高优先级） | 最高优先级队列，响应更快 |

**健康检查与降级机制：**

- 每30秒对DeepSeek API进行健康检查（发送test prompt）
- 连续3次检查失败 → 自动降级到Ollama
- 降级后每60秒尝试恢复DeepSeek
- 恢复成功后逐步切回DeepSeek（先切免费用户，再切付费用户）
- 降级事件写入日志，触发告警通知

### 3.6 成本控制策略

| 策略 | 说明 | 预估效果 |
|------|------|---------|
| LLM混合路由 | 免费/低级用户走本地Ollama，付费用户走DeepSeek | 减少API调用约80% |
| 内容预编译 | 常见场景的开场白、过渡语、通用反馈预生成缓存 | 60%走预设内容 |
| 本地优先训练积分 | 训练积分计算在客户端完成，服务端仅校验 | 减少服务端计算压力 |
| 对话上下文压缩 | 超过10轮对话后，压缩历史上下文再发送LLM | 减少token消耗 |
| 缓存热门场景 | 热门场景的教练回复模板缓存在Redis | 减少重复LLM调用 |

---
